# Common settings
server.port=8080
spring.application.name=pims-backend

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# sqlserver = SQL Server ProjectSWP391 (default) | h2 = local file DB for offline dev
spring.profiles.active=${SPRING_PROFILES_ACTIVE:sqlserver}
